spring:
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: localhost
      discovery:
        server-addr: localhost
    alibaba:
      seata:
        tx-service-group: tx_shop