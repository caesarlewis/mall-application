spring:
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: localhost
      discovery:
        server-addr: localhost
    alibaba:
      seta:
        tx-service-group: tx_shop